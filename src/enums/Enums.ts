export const enum ButtonEvents {
    Up = 'up',
    Down = 'down',
    Over = 'over',
    Out = 'out',
    InOut = 'InOut',
}

export const enum ButtonStateNames {
    Up = 'up',
    Down = 'down',
    Over = 'over',
    Disabled = 'disabled',
}
